class Solution:
    def findDisappearedNumbers(self, nums):
        num_set = set(nums)
        len_1 = len(nums)
        res = []
        for x_1 in range (1, len_1 + 1):
            if x_1 not in num_set:
                res.append(x_1)
        return res
